1 - Create a component that renders this JSX:

<div className='book'> 
  <div className='title'> 
    The Title 
  </div> 
  <div className='author'> 
    The Author 
  </div> 
  <ul className='stats'> 
    <li className='rating'> 
      5 stars 
    </li> 
    <li className='isbn'> 
      12-345678-910 
    </li> 
  </ul> 
</div>

2 - Return the appropiate JSX from this component, so that if username is undefined or null, it renders "Not Logged In". 
If it is a string, render * Hello { username } *

function Greeting() { 
  return (
    <React.Fragment>
      <div>
        Hello {} 
      </div> 
    </React.Fragment>
  )
} 
<br> 

3 - Create a table component called Table that renders a table with 1 row, and 3 columns and any data you like. Make sure 
it works. Then create a component called Data that renders the 3 columns, and replace the 3 
<td></td> with the 
<Data /> component.


function Table() {
  return (
    <table>
      <tr>
        <Data />
        <Data />
        <Data />
      </tr>
    </table>  
  )
}
function Data() {
  return (
    <td>{props.children}</td>
    <td>{props.children}</td>
    <td>{props.children}</td>
  )